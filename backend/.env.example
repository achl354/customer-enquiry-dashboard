# Copy to .env and fill in for live mailbox polling / AI classification.
# Leave blank to run in seed/demo mode with the rule-based classifier only.

PORT=4000

# Azure AD app registration (see README.md "Live ingestion setup")
TENANT_ID=
CLIENT_ID=
CLIENT_SECRET=

# The shared mailbox to poll, e.g. sales@jdhealthcare.com.au
MAILBOX=

# Cron expression for how often to poll the mailbox. Defaults to every
# minute ('* * * * *') if left blank. Loosen this (e.g. '*/5 * * * *' for
# every 5 minutes) if per-minute polling is more than the mailbox needs.
POLL_CRON_EXPRESSION=

# Anthropic API key for AI-based triage classification (see README.md "AI classification").
# Leave blank to use the rule-based classifier only.
ANTHROPIC_API_KEY=

# HTTP Basic Auth in front of the whole dashboard. Leave both blank to run
# with no login (fine for local dev). Set both once this is showing real
# customer/health-department correspondence, not just seed data.
DASHBOARD_USERNAME=
DASHBOARD_PASSWORD=
